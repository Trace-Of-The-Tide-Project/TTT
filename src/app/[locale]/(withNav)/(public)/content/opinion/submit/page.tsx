import OpinionSubmitForm from "@/components/content/opinion/OpinionSubmitForm";

/** `/content/opinion/submit` — FR-OPN-03: permanently open, public form, no login required. */
export default function OpinionSubmitPage() {
  return <OpinionSubmitForm />;
}
